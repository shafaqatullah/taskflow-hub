Enable GitHub Dependabot and add a `dependency-review` job on pull requests targeting `main`.

### Acceptance criteria
- [ ] `.github/dependabot.yml` configured for pub and GitHub Actions
- [ ] `dependency-review.yml` workflow on pull requests
- [ ] README mentions dependency hygiene

**Suggested branch:** `chore/dependabot`
